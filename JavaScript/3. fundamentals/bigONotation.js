function smallest ( arr ) {
    let min = arr[0];
    for( let element of arr ) {
        if( element < min ) {
            min = element;
        }
    }
    return min;
}
smallest( [2,5,6,12,14,28,37,41,44,45] );


function smallest( sortedArr ) {
    return sortedArr[0];
}
smallest( [2,5,6,12,14,28,37,41,44,45] );

for(let i=0; i < SpeechRecognitionResultList.length-1; j > 0; j--) {
    console.log(i, value, j);
}


